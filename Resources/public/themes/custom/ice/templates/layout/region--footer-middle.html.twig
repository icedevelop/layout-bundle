{#
/**
* @file
* Theme override to display footer_middle region.
*
* Available variables:
* - content: The content for this region, typically blocks.
* - attributes: HTML attributes for the region div.
* - region: The name of the region variable as defined in the theme's
*   .info.yml file.
*
* @see template_preprocess_region()
*/
#}

{% set container_classes = [] %}

{% if content %}
  <div{{ attributes.addClass(container_classes) }}>
    {% if footer_settings.footer_middle_one_title %}
      <h2 class="Footer-blockTitle Grid-cell">{{ footer_settings.footer_middle_one_title }}</h2>
    {% endif %}
    {{ content }}
    {{ social_locations.footer_middle_one }}
  </div>
{% endif %}
